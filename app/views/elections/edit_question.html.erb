<div class="main-body">

  <%=

    render 'layouts/question-update-form', locals: {
      submit_path: update_question_path(e_id: @election_id, q_id: @question.id),
      question_name: @question.question_name,
      question_description: @question.question_description,
      options: @question.options.map { |opt| opt.name }.join(", "),
      button_text: "Update",
      method: :patch
    }

  %>

</div>